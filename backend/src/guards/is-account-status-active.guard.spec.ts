import { IsAccountStatusActiveGuard } from './is-account-status-active.guard';

describe('IsAccountStatusActiveGuard', () => {
  it('should be defined', () => {
    expect(new IsAccountStatusActiveGuard()).toBeDefined();
  });
});
